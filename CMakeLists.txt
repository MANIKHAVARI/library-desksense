# This file tells ESP-IDF how to build the LibraryDeskSense project.
# It also sets the minimum CMake version that we need to build the project.

cmake_minimum_required(VERSION 3.16)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(LibraryDeskSense)